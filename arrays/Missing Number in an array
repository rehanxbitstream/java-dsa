//268. Find missing number in an array
public static int missingNumber(int [] arr){
  int xor1 = 0;
  int xor2 = 0;

  for(int i = 0;i<arr.length;i++){
  //Calculating XOR of all array element
  xor1^=arr[i];
  //Calculating XOR of 1 to N elements;
  xor2^=(i+1);
  }

  return xor1^xor2;
}
